// The GitHub Pages custom domain. bowenxue2005.github.io, www.bowenxue.me and
// the http:// variants all 301 here, so canonical/og/sitemap URLs must use
// this exact host — pointing them at a redirecting host tells crawlers the
// canonical URL is one that redirects away.
export const siteUrl = 'https://bowenxue.me';
